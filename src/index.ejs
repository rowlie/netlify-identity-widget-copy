<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Auth Widget Demo</title>
    <!-- Add any other necessary CSS or JS files here -->
</head>
<body>
    <div class="main">
        <div class="header">
            <h2><a href="https://www.netlify.com">Netlify Identity</a></h2>
            <h1>Auth widget demo</h1>
        </div>

        <div class="hands-on-area">
            <div class="single-button">
                <span class="label">Single button example:</span>
                <div data-netlify-identity-button></div>
            </div>

            <div class="multiple-buttons">
                <span class="label">Multiple buttons example:</span>
                <div data-netlify-identity-menu></div>
            </div>
        </div>
    </div>

    <!-- Script to handle authentication and redirection -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Listen for the 'identity:signedin' event
            window.addEventListener('identity:signedin', function(event) {
                // Redirect the user to the logged-in page
                window.location.href = '/logged-in.html'; // Adjust the path as needed
            });

            // Optionally, listen for the 'identity:signup' event if you want to redirect users after signing up
            window.addEventListener('identity:signup', function(event) {
                // Redirect the user to the logged-in page
                window.location.href = '/logged-in.html'; // Adjust the path as needed
            });
        });
    </script>
</body>
</html>
